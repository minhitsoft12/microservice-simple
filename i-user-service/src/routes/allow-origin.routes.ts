import { Router } from "express";

const allowOriginRoutes = Router();

// allowOriginRoutes.get("/", allowOriginController.getAllUsers);
// allowOriginRoutes.post("/", allowOriginController.createUser);
// allowOriginRoutes.get("/:id", allowOriginController.getUserById);
// allowOriginRoutes.put("/:id", allowOriginController.updateUser);
// allowOriginRoutes.delete("/:id", allowOriginController.deleteUser);

export default allowOriginRoutes;
